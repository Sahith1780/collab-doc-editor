version: '3.8'

services:
  syncdoc:
    build: .
    container_name: syncdoc-collaborative-editor
    ports:
      - "3000:3000"
    volumes:
      - ./server/storage:/app/server/storage
    environment:
      - PORT=3000
    restart: unless-stopped
